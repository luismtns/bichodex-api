class Animal {
  constructor(id, name, description, dreams, number) {
    (this.id = id),
      (this.name = name),
      (this.description = description),
      (this.dreams = dreams),
      (this.number = number);
  }
}

export default Animal;
